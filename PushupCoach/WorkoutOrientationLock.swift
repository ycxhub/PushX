import SwiftUI

// Portrait-only app. This file is intentionally empty — no orientation switching needed.
// Kept to avoid removing from the Xcode project file.

extension View {
    func workoutLandscapeWhenActive(_ active: Bool) -> some View {
        self
    }
}
